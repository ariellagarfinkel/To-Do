import React from "react";
function CreateToDo() {
    return <h1>Create TO Do</h1>
}



{/* 
      const handleSubmit = (event) => {
        event.preventDefault();
        handleCreateToDo({
          // the form
        }, () => {
          console.log("To Do created successfully ")
        })
      } */}

            {/* <form onSubmit={handleCreateToDo}> */}
                {/* </form> */}

export default CreateToDo;